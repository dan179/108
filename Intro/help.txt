# install packagespython -m pip install <name>
# create the venv
python -m venv venv
# activate the venv


win:
venv\Scripts\activate

mac:
source venv/bin/activate

start server:
python (name server).py
